#!/usr/bin/env bash

# Possible usage:
#   >>> nohup ./run.sh &
#
# To monitor:
#   >>> watch -n 5 "ps -ef | grep mfuMain.py"
#   or
#   >>>tail -f log.enkf

START=$(date +%s.%N)

dafi.py dafi.in &> log.dafi

END=$(date +%s.%N)
DIFF=$(echo "$END - $START" | bc)
echo $DIFF  > log.time

